pipeline {
    agent any
    parameters{
        string(name: 'FROM_BUILD', defaultValue: '', description: 'Build Source')
    }
    stages {
        stage("Build smoke") {
            steps {
                echo "Hello Build!"
            }
        }
        stage("Test") {
            steps {
                echo "Hello Test!"
            }
        }
    }
}
